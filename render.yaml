databases:
  - name: groundwater-ai-db
    databaseName: groundwater_ai
    user: groundwater_ai
    plan: free

services:
  - type: web
    name: groundwater-ai-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: groundwater-ai-db
          property: connectionString

